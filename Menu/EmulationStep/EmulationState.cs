namespace Universal_Turing_Machine {
    public enum EmulationState {
        INTRO,
        VALUE_SELECTION,
        MODE_SELECTION,
        OVERVIEW,
        RUNNING,
        FINNISHED,
        END,
    }
}
